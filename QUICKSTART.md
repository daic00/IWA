# IWA SUWS 2025 - 快速开始指南

## 系统功能概览

本系统包含完整的用户认证和管理功能：

### 用户功能
- ✅ 用户注册（邮箱唯一性验证）
- ✅ 用户登录
- ✅ 个人中心
- ✅ 修改密码（需要旧密码）
- ✅ 忘记密码（联系管理员）

### 管理员功能
- ✅ 管理员登录
- ✅ 查看所有用户
- ✅ 搜索用户
- ✅ 添加用户
- ✅ 编辑用户信息
- ✅ 删除用户
- ✅ 重置用户密码
- ✅ 用户统计

## 快速部署（3步）

### 第一步：安装依赖

打开终端（PowerShell），进入 server 目录：

```powershell
cd server
npm install
```

### 第二步：初始化数据库

```powershell
npm run init-db
```

这会创建：
- SQLite 数据库文件 `database/conference.db`
- 用户表和日志表
- 默认管理员账号

### 第三步：启动服务器

```powershell
npm start
```

或者使用开发模式（自动重启）：

```powershell
npm run dev
```

## 访问系统

服务器启动后，打开浏览器访问：

### 前端页面
- **首页**: http://localhost:3000
- **用户登录**: http://localhost:3000/login.html
- **用户注册**: http://localhost:3000/register.html
- **管理员登录**: http://localhost:3000/admin/login.html

### 默认管理员账号
```
用户名：admin
密码：Admin@2025
```

⚠️ **首次登录后请立即修改密码！**

## 测试流程

### 测试用户注册和登录

1. 访问 http://localhost:3000/register.html
2. 填写注册信息：
   - Username: testuser
   - Password: 123456
   - Name: 测试用户
   - Organization: 同济大学
3. 点击 "Create Account"
4. 注册成功后自动跳转到个人中心
5. 尝试修改密码
6. 退出登录
7. 使用新密码重新登录

### 测试管理员功能

1. 访问 http://localhost:3000/admin/login.html
2. 使用管理员账号登录：
   - Username: admin
   - Password: Admin@2025
3. 查看用户列表（应该能看到刚才注册的测试用户）
4. 测试搜索功能
5. 测试添加用户
6. 测试编辑用户信息
7. 测试重置密码
8. 测试删除用户

## 文件结构

```
d:\Code\IWA\
├── index.html                    # 会议主页
├── login.html                    # 用户登录
├── register.html                 # 用户注册
├── reset-password.html           # 忘记密码（联系管理员）
├── profile.html                  # 用户个人中心
├── change-password.html          # 修改密码
├── admin/
│   ├── login.html               # 管理员登录
│   ├── users.html               # 用户管理页面
│   └── users.js                 # 用户管理逻辑
├── css/
│   ├── style.css                # 全局样式
│   ├── pages.css                # 页面样式
│   └── auth.css                 # 认证页面样式
├── server/
│   ├── server.js                # 主服务器
│   ├── package.json             # 项目配置
│   ├── .env.example             # 环境变量示例
│   ├── database/
│   │   ├── init.js              # 数据库初始化
│   │   └── conference.db        # SQLite数据库（自动生成）
│   ├── middleware/
│   │   └── auth.js              # 认证中间件
│   └── routes/
│       ├── auth.js              # 用户认证路由
│       └── admin.js             # 管理员路由
└── QUICKSTART.md                # 本文件
```

## API 端点

### 用户 API
- `POST /api/auth/register` - 注册
- `POST /api/auth/login` - 登录
- `GET /api/auth/me` - 获取当前用户信息
- `POST /api/auth/change-password` - 修改密码
- `POST /api/auth/logout` - 退出登录

### 管理员 API
- `POST /api/admin/login` - 管理员登录
- `GET /api/admin/check` - 检查管理员权限
- `GET /api/admin/users` - 获取所有用户
- `POST /api/admin/users` - 创建用户
- `PUT /api/admin/users/:id` - 更新用户
- `DELETE /api/admin/users/:id` - 删除用户
- `POST /api/admin/reset-password` - 重置用户密码

## 技术栈

- **前端**: HTML5, CSS3, JavaScript (原生)
- **后端**: Node.js + Express
- **数据库**: SQLite (better-sqlite3)
- **会话**: express-session
- **密码加密**: bcrypt

## 安全特性

- ✅ 密码加密存储（bcrypt）
- ✅ 邮箱唯一性验证
- ✅ Session 会话管理
- ✅ 防止 SQL 注入（参数化查询）
- ✅ 管理员操作日志
- ✅ 密码强度验证（最少6字符）

## 常见问题

### Q: 端口 3000 被占用怎么办？

修改 `server/.env` 文件中的 PORT：
```
PORT=8080
```

或者直接指定端口启动：
```powershell
$env:PORT=8080; npm start
```

### Q: 如何重置管理员密码？

1. 停止服务器
2. 删除 `server/database/conference.db` 文件
3. 重新运行 `npm run init-db`

### Q: 如何备份数据？

直接复制 `server/database/conference.db` 文件即可。

### Q: 如何查看数据库内容？

使用 DB Browser for SQLite 打开 `conference.db` 文件。

下载地址：https://sqlitebrowser.org/

### Q: 用户名唯一性在哪里验证？

在后端 `server/routes/auth.js` 的注册接口中：
```javascript
// 检查用户名是否已存在（唯一性校验）
const existingUser = db.prepare('SELECT id FROM users WHERE username = ?').get(username);
if (existingUser) {
    return res.json({ 
        success: false, 
        message: 'This username is already taken' 
    });
}
```

同时数据库表结构中也有唯一约束：
```sql
CREATE TABLE users (
    username TEXT UNIQUE NOT NULL,
    ...
);
```

## 开发建议

### 修改配置

编辑 `server/.env` 文件（如果不存在，复制 `.env.example`）：

```env
PORT=3000
SESSION_SECRET=your-secret-key
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin@2025
```

### 开发模式

使用 nodemon 自动重启：
```powershell
npm run dev
```

### 调试

查看服务器日志，所有错误都会在控制台输出。

## 部署到生产环境

### 检查清单

- [ ] 修改 SESSION_SECRET 为随机字符串
- [ ] 修改默认管理员密码
- [ ] 设置 NODE_ENV=production
- [ ] 配置 HTTPS
- [ ] 定期备份数据库
- [ ] 设置防火墙规则

### 使用 PM2 部署

```powershell
npm install -g pm2
cd server
pm2 start server.js --name iwa-conference
pm2 save
pm2 startup
```

## 获取帮助

如有问题，请查看：
- `server/README.md` - 详细的后端文档
- 服务器控制台输出 - 错误日志
- 浏览器开发者工具 - 网络请求和错误

## 下一步

1. ✅ 测试所有功能
2. ✅ 修改管理员密码
3. ✅ 自定义页面样式和内容
4. ✅ 备份数据库
5. ✅ 部署到生产服务器

祝您使用愉快！🎉
